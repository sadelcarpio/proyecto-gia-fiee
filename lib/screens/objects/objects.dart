import 'package:flutter/material.dart';
// Jean
class ObjectsRecognitionPage extends StatelessWidget {
  const ObjectsRecognitionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
